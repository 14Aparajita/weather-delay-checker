{
  "name": "weather-delay-checker",
  "version": "1.0.0",
  "description": "Checks each order's city against OpenWeatherMap and flags delivery delays, with parallel fetching and error handling.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
